# 项目审核记录（2026-09-24）

## 审核范围

- 代码仓库：`E:\Research\CityInspection_GOC`
- 当前阶段：S1 三随机种子 × 两目标位置受控批量实验
- 代码与配置：36 个 Python 文件、9 个 PowerShell 文件、18 个 YAML 文件、5 个 JSON 文件
- 正式批次：`E:\CarlaAirData\CityInspection_GOC\e3_s1_multiseed_batch\S1_MULTI_SEED_3X2_20260923T125945Z`

## 发现并修复的问题

1. 批量汇总器此前始终返回成功码，上层存在将缺失或失败结果误标为 `PASS` 的风险。现已要求“实际结果数等于冻结计划数，且每组均为 PASS”才返回成功。
2. 批量运行器此前会在汇总异常时仍写入 `LATEST.txt`。现仅在完整批次通过时更新最新结果指针。
3. 批量配置关于 A/B 路线长度的注释写反。已改为不依赖具体长短关系的准确说明。
4. README 缺少最新批量运行入口、6/6 验收结论和结果文档索引。现已补齐。
5. 新增 5 项批量汇总测试，覆盖完整通过、缺少实验、科学失败、缺少计划数以及命令行失败返回码。

上述问题均不改变已经完成的六组科学结果。六份 `validation_report.json` 已分别确认状态为 `PASS`，每组 36 项检查均无失败。

## 第二轮审核结果

- Python 语法检查：通过。
- PowerShell 语法检查：9/9 通过。
- YAML/JSON 解析：全部通过。
- 自动化测试：21/21 通过。
- Python 依赖一致性：`pip check` 通过。
- 敏感信息扫描：未发现密钥、令牌或密码。
- Git 差异检查：未发现空白错误。
- 正式批次重新汇总：计划 6 组、可用 6 组、通过 6 组、失败 0 组，`batch_complete=true`、`all_passed=true`。
- 缺组负向测试：返回码为失败，不再更新 `LATEST.txt`。

## 清理结果

- 删除 9 个可再生成的 Python/pytest 缓存目录，约 0.18 MB。
- 复查清理白名单后，待清理目标为 0。
- 正式结果、模型权重和 `engineering_diagnostics` 启动异常审计记录均保留。

## 已知研究边界

- 绝对路径仍绑定当前 Windows 实验机，这是现阶段部署约束，不是本轮代码错误。
- 预训练模型权重不进入 Git；克隆后需准备相应公开权重。
- 当前 6 组结果属于固定 Town10HD/Zone A 下的受控小批量鲁棒性验收，不能替代跨地图、大样本或最终感知模型泛化实验。
