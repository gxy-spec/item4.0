# 项目审核记录（2026-09-22）

## 审核范围

- 代码仓库：`E:\Research\CityInspection_GOC`
- 实验输出：`E:\CarlaAirData\CityInspection_GOC`
- 阶段：CI-E0、CI-E1、CI-E1 Full Route、CI-E1.1 Physical Fix、CI-E2 UAV Candidate Baseline、S0 Oracle Closed Loop及无消息负对照。

## 代码与配置检查

- Python源码编译检查通过。
- 自动化测试：10项全部通过。
- 6个PowerShell运行入口语法检查通过。
- YAML、JSON及JSON Schema均可解析，配置引用文件均存在。
- Python环境依赖一致性检查通过；审核时补充了此前漏写的直接依赖 `opencv-python>=5.0`。
- 未发现密钥、令牌等敏感信息；模型权重、缓存和大体量实验结果均由Git忽略。

## 正式结果完整性

- 7个正式回放视频均可解码，帧数、帧率和分辨率有效。
- 抽查17个图像与可视化文件，均可正常读取。
- 对正式结果共检查29,757个文件；解析40个JSON、121,686条JSONL记录、33,661行CSV和7,402个NPY数组，未发现结构损坏。
- 5份现行验收报告均为 `PASS`，合计104项检查中无失败项。
- 无消息负对照的 `communication_events.jsonl` 与 `messages.jsonl` 为0字节，这是“未产生任何消息”的预期语义，不属于数据损坏。

## 发现的问题与影响

1. **Windows依赖清单漏项（已修复）**：感知与视频脚本直接导入 `cv2`，但依赖文件未显式列出OpenCV。已加入 `opencv-python>=5.0`。
2. **文档索引不完整（已修复）**：README未列出CI-E2与S0正式文档。现已补齐，并写明Oracle/感知边界。
3. **CI-E2误检仍高（未解决，属于下一阶段工作）**：可见目标召回率98.04%，但红色候选精度代理仅20.67%。该阶段只证明“目标能进入候选集”，不能声称已完成可靠目标识别或S1闭环。
4. **CI-E2颜色指标容易误读（已澄清）**：100%颜色正确率只针对已与Oracle目标匹配的命中帧，不是所有候选的总体颜色分类准确率。
5. **S0时延不是网络实测（已澄清）**：50 ms为配置的一仿真步确定性时延，只用于验证消息队列与状态机。
6. **模型复现依赖本地权重（已记录）**：权重不进入Git，克隆项目后需准备配置指定的模型，或允许Ultralytics按模型名获取公开权重。
7. **路径具有本机绑定（非当前错误）**：冻结配置中的D盘/E盘绝对路径适用于当前实验机；迁移到其他计算机时必须修改配置。

## 保留的基准结果

- CI-E0正式预览。
- CI-E1正式20秒同步验收与最终通过的5秒smoke结果。
- CI-E1 Full Route 150秒完整航线结果。
- CI-E1.1 Physical Fix最终30秒结果（也是CI-E2输入）。
- CI-E2最终候选基线结果 `...T022645Z`。
- S0 Oracle正式正向闭环 `...T063718Z` 与无消息负对照 `...T061808Z`。

## 清理策略

清理只针对明确失败、异常中止或已被最终结果替代的运行，以及可再生的Python/pytest缓存；正式结果、最终smoke结果、模型权重、配置、文档和源代码均不在清理范围内。受控入口为：

```powershell
.\scripts\Clear-ObsoleteArtifacts.ps1        # 仅预览
.\scripts\Clear-ObsoleteArtifacts.ps1 -Apply # 按显式白名单清理
```

脚本使用固定白名单并验证每个目标都位于批准的数据根目录或仓库根目录之下，拒绝对根目录本身执行操作。

## 本次清理结果

- 已删除12个明确失败、异常中止或被最终结果替代的实验目录。
- 已删除12个可再生的Python/pytest缓存目录。
- 实际清理约529.09 MB；清理后再次预览白名单，待清理目标为0。
- 上述7个保留基准结果均已逐项确认仍存在。
